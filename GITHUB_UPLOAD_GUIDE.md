# GitHub Upload Guide

Repository name:

```text
vendor-compliance-portal
```

Description:

```text
Laravel vendor compliance and document approval portal with RBAC, vendor onboarding, private document uploads, review workflows, expiry monitoring, reports, and audit logs.
```

Create a new GitHub repository first. Do not tick README, .gitignore, or license because this project already includes them.

## Upload commands

```powershell
cd "C:\Users\showmik\Downloads\vendor-compliance-portal-github-ready-final"

git init
git add .
git commit -m "Vendor compliance and document approval portal"
git branch -M main
git remote add origin https://github.com/SAHARIARSHOWMIK/vendor-compliance-portal.git
git push -u origin main
```

## Recommended topics

```text
laravel
php
vendor-management
compliance
document-approval
rbac
audit-log
workflow-automation
mysql
full-stack
```

## After pushing

Check:

```text
README renders correctly
Actions tab latest CI run completes
No .env file is uploaded
No vendor/ or node_modules/ folders are uploaded
```
